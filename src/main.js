'use strict'

/**
 *  We're going to start off with a vanilla react savor install
 *  that we will extend to work with React Native apps, if necessary
 **/
var savor = require("react-savor")

module.exports = savor
